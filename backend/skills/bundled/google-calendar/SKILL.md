---
name: google-calendar
description: "Navigate and manage Google Calendar using keyboard shortcuts — switch views, create events, navigate dates, edit events, and access Tasks/Keep. Use when: user needs to create calendar events, check schedule, switch between day/week/month views, or navigate dates. Environment: Google Calendar web app in a browser (calendar.google.com). IMPORTANT: Keyboard shortcuts must be enabled in Calendar Settings first."
---

## Google Calendar — Keyboard Shortcuts

Google Calendar is accessed at calendar.google.com. Keyboard shortcuts must be turned on in Settings.

### How to Enable Keyboard Shortcuts

1. Open Google Calendar → click the **gear icon** (top-right) → **Settings**
2. Check **Enable keyboard shortcuts**
3. Click **Save**

> Press `?` (Shift + /) inside Calendar to see the shortcut help overlay.

---

### Navigation — Move Around the Calendar

| Action | Windows | macOS |
|---|---|---|
| Previous time period (day/week/month) | `p` or `k` | `p` or `k` |
| Next time period (day/week/month) | `n` or `j` | `n` or `j` |
| Go to today | `t` | `t` |
| Go to a specific date | `g` (opens date picker) | `g` |

### Change Your Calendar View

| Action | Windows | macOS |
|---|---|---|
| Day view | `1` or `d` | `1` or `d` |
| Week view | `2` or `w` | `2` or `w` |
| Month view | `3` or `m` | `3` or `m` |
| Custom view (X days) | `4` or `x` | `4` or `x` |
| Schedule / list view | `5` or `a` | `5` or `a` |
| Year view | `6` or `y` | `6` or `y` |

### Create & Edit Events

| Action | Windows | macOS |
|---|---|---|
| Create new event | `c` | `c` |
| View event details | `e` (with event selected) | `e` |
| Delete event | `Delete` or `Backspace` | `Delete` or `Fn + Delete` |
| Undo last action | `z` | `z` |
| Save event (in event editor) | `Ctrl + s` | `⌘ + s` |
| Back to calendar grid | `Esc` | `Esc` |

### Quick Actions

| Action | Windows | macOS |
|---|---|---|
| Search | `/` | `/` |
| Open Settings | `s` | `s` |
| Show keyboard shortcuts help | `?` (Shift + /) | `?` (Shift + /) |
| Go to side panel (Tasks, Keep, etc.) | `Alt + Shift + .` | `Option + Shift + .` |
| Return from side panel | `Alt + Shift + ,` | `Option + Shift + ,` |
| Refresh calendar | `r` | `r` |

### View Tasks & Keep

| Action | Windows | macOS |
|---|---|---|
| Open Tasks side panel | Click Tasks icon or `Alt + Shift + .` then navigate | Click Tasks icon or `Option + Shift + .` then navigate |
| Open Google Keep side panel | Click Keep icon or `Alt + Shift + .` then navigate | Click Keep icon or `Option + Shift + .` then navigate |

### Tips

- **Single-key shortcuts:** Most Calendar shortcuts are single-key presses (no Ctrl needed).
- **Date navigation:** `p`/`n` move backward/forward by whatever view you're in (e.g., one week in week view, one month in month view).
- **Quick event creation:** Press `c` to open the event creation dialog, type details, and press `Ctrl + s` (Windows) or `⌘ + s` (macOS) to save.
- **Go to date:** Press `g`, type a date (e.g., "12/25/2025"), and press Enter.
- To switch views quickly, just press the number keys `1`–`6`.
- On macOS, `⌘` replaces `Ctrl` and `Option` replaces `Alt` for modifier-key shortcuts.
